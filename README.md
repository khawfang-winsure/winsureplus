# WIN SURE PLUS — ระบบติดตามลูกค้าผ่อนเครื่อง

เว็บกลางสำหรับธุรกิจผ่อน iPhone ผ่านร้านค้าพาร์ทเนอร์ — กรอกข้อมูล **ครั้งเดียว** แล้วได้ครบ:
เก็บข้อมูลลูกค้า/ร้านค้า + สร้างข้อความสรุปยอดโอน + สร้างข้อความอีเมล + ติดตามสถานะการชำระ

> มาแทน Google Sheets หลายชีต ที่ไม่ real-time และตรวจสอบยาก

---

## สถานะ: โค้ดครบทุกฟีเจอร์ (code-complete)

✅ หน้าภาพรวม (Dashboard) สรุปตัวเลข
✅ เพิ่ม/แก้สัญญา — กรอกครั้งเดียว คำนวณยอดสด + สร้างข้อความสรุปยอด/อีเมล (คัดลอก)
✅ ลูกค้าทั้งหมด + รายละเอียดสัญญา (ตารางงวดผ่อน + ยืนยันชำระ)
✅ รอสรุปยอด (รวมหลายร้าน) / รอส่งอีเมล — กันส่งซ้ำ
✅ ลูกค้าล่าช้าแยกกลุ่ม / ถึงกำหนด / ส่งจดหมาย / คืนเครื่อง 3 กรณี
✅ ตั้งค่า (แก้ร้าน/ตัวเลือก) + login (admin/staff) + กระดิ่งแจ้งเตือน
✅ ฐานข้อมูล + ระบบอัตโนมัติรายวัน (migrations 0001–0005, ทดสอบแล้ว)

> ⚠️ **ก่อนใช้งานจริง ต้องตั้งค่า Supabase ก่อน** — ดู [`supabase/README-วิธีเชื่อม.md`](supabase/README-วิธีเชื่อม.md)
> (รัน migrations 0002–0005 + สร้าง user + ตั้ง admin) มิฉะนั้นเปิดเว็บจะเจอ login แล้วเข้าข้อมูลไม่ได้

---

## วิธีเปิดรันในเครื่อง

ต้องมี [Node.js](https://nodejs.org/) (เวอร์ชัน 18 ขึ้นไป) ก่อน

```bash
npm install      # ติดตั้งครั้งแรกครั้งเดียว
npm run dev      # เปิดเว็บที่ http://localhost:5173
```

ก่อน deploy ขึ้นจริง ให้รัน `npm run build` เช็คว่าไม่มี error

---

## เทคโนโลยีที่ใช้

| ส่วน | เครื่องมือ |
|------|-----------|
| หน้าเว็บ | React + Vite + TypeScript + Tailwind CSS |
| ฐานข้อมูล (ขั้นต่อไป) | Supabase (Postgres) |
| โฮสต์ (ขั้นต่อไป) | Vercel |

---

## โครงไฟล์

```
src/
  components/    ส่วน UI ที่ใช้ซ้ำ (เมนู, การ์ด, ปุ่ม, กล่องคัดลอก)
  pages/         แต่ละหน้าในเมนู (เพิ่มสัญญา, ลูกค้าทั้งหมด, ...)
  lib/
    types.ts     พิมพ์เขียวข้อมูล (จะ map กับตาราง Supabase)
    calc.ts      สูตรคำนวณ (ยอดโอน, วันครบกำหนด, ค่าปรับ)
    messages.ts  ตัวสร้างข้อความสรุปยอด/อีเมล
    format.ts    จัดรูปแบบตัวเลข/วันที่/ช่วงอายุ
    mockData.ts  ข้อมูลตัวอย่าง (จะแทนด้วยข้อมูลจริงจาก Supabase)
```

---

## การตั้งค่าก่อนใช้งานจริง

1. **เชื่อม Supabase** — ทำตาม [`supabase/README-วิธีเชื่อม.md`](supabase/README-วิธีเชื่อม.md) (รัน migrations + ใส่กุญแจใน `.env`)
2. **ย้ายข้อมูลเก่า** (ถ้ามี) — ดู [`supabase/README-ย้ายข้อมูลเก่า.md`](supabase/README-ย้ายข้อมูลเก่า.md)

## หมายเหตุ

- กฎธุรกิจสำคัญ (สูตรยอดโอน, วันครบกำหนด clamp สิ้นเดือน, ค่าปรับ 700฿/งวด ฯลฯ)
  อยู่เป็นคอมเมนต์ใน `src/lib/calc.ts` และ SQL ใน `supabase/migrations/`
- ทุก migration ทดสอบกับ Postgres จริง (PGlite) แล้วว่ารันผ่าน
- ต่อยอดอนาคต: รายงานวัดผลร้านค้า, ส่งอีเมล/จดหมายอัตโนมัติจริง (ตอนนี้คัดลอกไปวาง)
