-- 0141: ตั้งค่าความยาวบรรทัดสรุปไฟล์แนบรูปในอีเมลบริษัท (send-company-email) — ลดความยาวอีเมลตามที่คุณเตยขอ
-- ค่าที่รองรับ 3 แบบ (เก็บเป็น text ธรรมดา ตรวจ/แปลงค่าไม่รู้จักเป็น 'short' ฝั่ง Edge Function):
--   short = บรรทัดเดียว "แนบรูปเอกสาร N ใบ" (ค่าเริ่มต้น — Gmail แสดงชื่อไฟล์แนบเองอยู่แล้ว ไม่ต้องแจกแจงซ้ำ)
--   full  = แจกแจงรายช่องแบบเดิม เช่น "แนบรูปเอกสาร 22 ใบ (13 ช่อง): หน้าบัตรประชาชนลูกค้า (1), ..."
--   off   = ไม่ต่อบรรทัดนี้เลย
-- Additive — app_settings ใช้ on conflict do nothing (ไม่ทับค่าที่ admin แก้เองไปแล้ว)

insert into app_settings (key, value, description) values
  ('media_email_attach_summary', 'short',
   'รูปแบบบรรทัดสรุปไฟล์แนบในอีเมลบริษัท: short=บรรทัดเดียว "แนบรูปเอกสาร N ใบ" (ค่าเริ่มต้น) / full=แจกแจงรายช่องแบบเดิม / off=ไม่มีบรรทัดนี้')
on conflict (key) do nothing;

-- ============================================================================
-- Smoke SQL (รันมือหลัง apply เพื่อ verify — ไม่ได้รันอัตโนมัติในไฟล์นี้)
-- ============================================================================
-- 1) seed ค่าเริ่มต้น 'short':
-- SELECT key, value, description FROM app_settings WHERE key = 'media_email_attach_summary';
--
-- 2) ไม่ทับค่าเดิมถ้ามีคนตั้งไว้ก่อนแล้ว (รัน migration ซ้ำ):
-- ค่าที่ได้ต้องเหมือนเดิม ไม่กลับไปเป็น 'short' ถ้าเคยเปลี่ยนเป็น full/off แล้ว
