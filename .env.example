# คัดลอกไฟล์นี้เป็น .env แล้วใส่กุญแจจาก Supabase
# วิธีหา: Supabase Dashboard > Project Settings > API
# (ห้ามใส่กุญแจจริงในไฟล์ .env.example นี้ เพราะไฟล์นี้ขึ้น GitHub — ใส่ใน .env เท่านั้น)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
